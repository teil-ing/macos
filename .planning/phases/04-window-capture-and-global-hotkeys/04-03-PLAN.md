---
phase: 04-window-capture-and-global-hotkeys
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: false
requirements:
  - CAPT-03
  - CAPT-04

must_haves:
  truths:
    - "Window capture from menu bar works end-to-end: click button, see dimming overlay with camera cursor, hover highlights window, click captures it"
    - "Desktop click during window selection captures the fullscreen of that display"
    - "All three hotkeys (Cmd+Shift+X, Cmd+Shift+S, Cmd+Shift+C) trigger the correct capture mode from another application"
    - "Escape cancels both region and window selection overlays triggered by hotkey"
    - "No Dock icon appears when hotkeys fire"
    - "Capture feedback (flash, sound, icon) fires for all capture modes regardless of trigger method"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete window capture and global hotkeys feature. All automated work from Plans 01 and 02 is verified by a real user testing the interactive flows.

Purpose: Confirms that both CAPT-03 (window capture by clicking) and CAPT-04 (global hotkeys) work correctly in practice.
Output: Human-verified phase completion.
</objective>

<execution_context>
@/Users/ruohki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruohki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-window-capture-and-global-hotkeys/04-01-SUMMARY.md
@.planning/phases/04-window-capture-and-global-hotkeys/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify window capture and global hotkeys end-to-end</name>
  <files></files>
  <action>
Build and run the app for human verification. No code changes — this is a verification-only checkpoint.

The human will test the complete window capture and global hotkey flow across all scenarios listed in the verification steps.
  </action>
  <verify>
Human confirms all test scenarios pass by typing "approved".

**Window Capture from Menu Bar:**
1. Click the teil.ing menu bar icon to open the popover
2. Verify the "Window" button is now enabled (not grayed out)
3. Click "Window" — the popover should close
4. After ~200ms, a dimming overlay should appear on all displays with a camera cursor
5. Move the mouse over different windows — the hovered window should appear bright while everything else is dimmed
6. Click on a specific window — the overlay should dismiss, a white flash should appear at the window's position, a sound should play, and the menu bar icon should change to a checkmark for ~2 seconds
7. Verify the captured image has NO drop shadow and transparent corners

**Desktop Click:**
8. Trigger "Window" capture again from the menu bar
9. Click on the desktop (not on any window) — this should capture the fullscreen of that display instead
10. Verify flash, sound, and icon change fired

**Escape Cancel:**
11. Trigger "Window" capture again
12. Press Escape — the overlay should dismiss with NO capture, NO feedback

**Global Hotkeys:**
13. Switch focus to a different application (e.g., Finder, Safari, Terminal)
14. Press Cmd+Shift+X — region capture overlay should appear after ~200ms
15. Drag a region and release — verify capture feedback fires
16. Press Cmd+Shift+S — fullscreen capture should fire on the display where the cursor is
17. Press Cmd+Shift+C — window selection overlay should appear after ~200ms
18. Click a window to capture it — verify feedback fires

**No Focus Steal:**
19. While in another app, press Cmd+Shift+S — verify the other app stays focused after capture. teil.ing should NOT appear in the Dock or Cmd+Tab switcher.

**Cancel from Hotkey:**
20. Press Cmd+Shift+X from another app — region overlay appears
21. Press Escape — overlay dismisses, no capture occurs, other app retains focus
  </verify>
  <done>
All 21 test scenarios pass. Window capture produces shadow-free images with transparent corners. All three hotkeys trigger from non-teil.ing applications. No Dock icon or focus steal on any hotkey path. Escape cancels correctly in all modes.
  </done>
</task>

</tasks>

<verification>
Human has verified all interactive flows listed in the checkpoint task.
</verification>

<success_criteria>
- All 21 test scenarios pass
- Window capture produces shadow-free images with transparent corners
- All three hotkeys trigger from non-teil.ing applications
- No Dock icon or focus steal on any hotkey path
- Escape cancels correctly in all modes
</success_criteria>

<output>
After completion, create `.planning/phases/04-window-capture-and-global-hotkeys/04-03-SUMMARY.md`
</output>
