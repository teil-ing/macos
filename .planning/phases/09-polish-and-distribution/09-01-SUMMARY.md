---
phase: 09-polish-and-distribution
plan: 01
subsystem: testing
tags: [xctest, unit-tests, error-handling, nsdialert, screen-recording, xcodegen]

# Dependency graph
requires:
  - phase: 05-upload-pipeline
    provides: UploadError and UploadResponse types with LocalizedError conformance
  - phase: 02-onboarding-and-keychain
    provides: KeychainError type with LocalizedError conformance
  - phase: 03-capture-engine-region-and-fullscreen
    provides: PermissionService.checkScreenRecordingPermission() and openScreenRecordingSettings()
provides:
  - XCTest unit test target (teil.ing-clientTests) in project.yml and Xcode project
  - ErrorPathTests.swift covering all UploadError cases, KeychainError, and UploadResponse decoding
  - presentScreenRecordingDeniedAlert() in AppDelegate showing NSAlert with Open Settings button
  - Permission gate in all three capture methods (region, fullscreen, window)
affects: [09-02-distribution, 09-03-notarization]

# Tech tracking
tech-stack:
  added: [XCTest (unit test target)]
  patterns: [GENERATE_INFOPLIST_FILE=YES for test targets in xcodegen, permission-gate-before-capture pattern]

key-files:
  created:
    - Tests/ErrorPathTests.swift
  modified:
    - project.yml
    - teil.ing-client/App/AppDelegate.swift
    - teil.ing-client.xcodeproj/project.pbxproj

key-decisions:
  - "GENERATE_INFOPLIST_FILE=YES required in xcodegen test target settings — without it xcodebuild test fails code signing"
  - "presentScreenRecordingDeniedAlert() is a private @MainActor method on AppDelegate — NSAlert.runModal() is synchronous and must run on main thread"
  - "Permission check placed after popover dismiss delay but before overlay/capture — ensures clean UI state before blocking modal"

patterns-established:
  - "Permission gate pattern: await PermissionService.checkScreenRecordingPermission() → presentScreenRecordingDeniedAlert() → return, placed at top of each capture Task block"
  - "XCTest target in xcodegen: bundle.unit-test type, GENERATE_INFOPLIST_FILE=YES, @testable import teil_ing_client"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-02-18
---

# Phase 9 Plan 01: Error Paths and XCTest Infrastructure Summary

**XCTest unit test target with 11 passing error-path tests, plus NSAlert screen recording denial gate wired into all three capture methods**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-02-18T09:06:15Z
- **Completed:** 2026-02-18T09:08:26Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `teil.ing-clientTests` XCTest bundle target to project.yml; regenerated Xcode project via xcodegen
- Created `Tests/ErrorPathTests.swift` with 11 unit tests covering all 7 UploadError cases, KeychainError, and UploadResponse JSON decoding — all pass with 0 failures
- Added `presentScreenRecordingDeniedAlert()` to AppDelegate with NSAlert, "Open Settings" button wired to `PermissionService.openScreenRecordingSettings()`
- Wired `PermissionService.checkScreenRecordingPermission()` permission gate into all three capture methods (region, fullscreen, window) before overlay/capture logic

## Task Commits

Each task was committed atomically:

1. **Task 1: Add XCTest target and screen recording denial alert** - `0bedefe` (feat)
2. **Task 2: Write error path unit tests** - `c0e4572` (test)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `Tests/ErrorPathTests.swift` - 11 XCTest unit tests for UploadError, KeychainError, UploadResponse
- `project.yml` - Added teil.ing-clientTests bundle.unit-test target with GENERATE_INFOPLIST_FILE=YES
- `teil.ing-client/App/AppDelegate.swift` - Added presentScreenRecordingDeniedAlert(), permission checks in all three capture methods
- `teil.ing-client.xcodeproj/project.pbxproj` - Regenerated by xcodegen with test target

## Decisions Made
- `GENERATE_INFOPLIST_FILE=YES` is required in xcodegen test target settings — xcodebuild test fails code signing without it (auto-fixed during Task 1 verification)
- `presentScreenRecordingDeniedAlert()` uses `NSAlert.runModal()` (synchronous on main thread) — correct pattern for macOS @MainActor modal alerts
- Permission gate placed after popover dismiss delay, before overlay/capture logic — ensures UI is clear before the blocking permission check and potential modal

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added GENERATE_INFOPLIST_FILE=YES to test target settings**
- **Found during:** Task 1 (xcodebuild test run)
- **Issue:** Test target in project.yml lacked GENERATE_INFOPLIST_FILE=YES; xcodebuild test failed with "Cannot code sign because the target does not have an Info.plist file and one is not being generated automatically"
- **Fix:** Added `GENERATE_INFOPLIST_FILE: YES` to teil.ing-clientTests settings.base in project.yml; re-ran xcodegen
- **Files modified:** project.yml
- **Verification:** xcodebuild test ran all 11 tests with 0 failures after fix
- **Committed in:** `0bedefe` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — missing required build setting)
**Impact on plan:** Fix was necessary for tests to run at all. No scope creep.

## Issues Encountered
- Test target missing `GENERATE_INFOPLIST_FILE=YES` caused code signing failure on first test run — resolved immediately with auto-fix Rule 1

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- XCTest infrastructure is in place for future test additions in Phase 9 plans
- Screen recording denial alert is fully wired and functional
- All error cases have verified, non-empty user-readable descriptions
- Ready for Phase 9 Plan 02 (distribution/notarization)

## Self-Check: PASSED

- FOUND: Tests/ErrorPathTests.swift
- FOUND: project.yml (contains teil.ing-clientTests target)
- FOUND: AppDelegate.swift (4 occurrences of presentScreenRecordingDeniedAlert, 3 of checkScreenRecordingPermission)
- FOUND: commit 0bedefe (feat(09-01): add XCTest target and screen recording denial alert)
- FOUND: commit c0e4572 (test(09-01): add error path unit tests covering all UploadError cases)

---
*Phase: 09-polish-and-distribution*
*Completed: 2026-02-18*
