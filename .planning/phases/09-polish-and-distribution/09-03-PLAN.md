---
phase: 09-polish-and-distribution
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified: []
autonomous: false

requirements: []

must_haves:
  truths:
    - "User confirms screen recording denial alert appears and 'Open Settings' button opens System Settings"
    - "User confirms existing upload error flows (error icon, popover banner, Retry) work correctly"
    - "User confirms build-dmg.sh and release.yml are reviewed and ready for use when Developer ID certificate is available"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 9 deliverables: error path behavior, build pipeline review, and CI configuration review.

Purpose: Confirm that error paths show visible, actionable feedback and that the build/distribution infrastructure is correct before the phase is marked complete.
Output: Human-verified Phase 9 completion.
</objective>

<execution_context>
@/Users/ruohki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruohki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-distribution/09-01-SUMMARY.md
@.planning/phases/09-polish-and-distribution/09-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify error paths and review build infrastructure</name>
  <files></files>
  <action>
Present the following verification checklist to the user. This is a human verification checkpoint -- no automated work to perform. All implementation was completed in Plans 01 and 02.

Phase 9 deliverables to verify:
1. Screen recording denial NSAlert with "Open Settings" button (wired into all 3 capture methods)
2. XCTest unit tests for all error handling paths (UploadError, KeychainError, UploadResponse decoding)
3. Entitlements file audit (documented, only hardened-runtime)
4. ExportOptions.plist for Developer ID export
5. build-dmg.sh -- full pipeline script (archive, sign, notarize, staple, DMG, GitHub Release)
6. DMG background image (540x380 branded PNG)
7. GitHub Actions release.yml (automated build+notarize+release on v* tag push)

**Error Path Verification (requires running the app):**

1. **Screen Recording denial alert**: Revoke Screen Recording permission for teil.ing in System Settings > Privacy & Security > Screen Recording. Launch the app, then try any capture mode (Region/Fullscreen/Window). Expected: A modal NSAlert appears with "Screen Recording Access Required" message and two buttons: "Open Settings" and "Not Now". Click "Open Settings" -- it should open System Settings to the Screen Recording pane.

2. **Missing API key on launch**: Open Keychain Access, find the `ing.teil.client` entry under "api-key", delete it. Relaunch the app. Expected: The onboarding window appears (same as first launch) prompting for an API key.

3. **Upload error paths** (verify existing UI): With the app running and API key set, try an upload. If it succeeds, the existing error UI is not triggered -- this is expected. To verify error UI still works: temporarily set an invalid API key, capture a screenshot, and observe: Expected: Error icon appears in menu bar, popover shows red error banner with "API key is invalid or expired" message and a "Retry Upload" button.

4. **Unit tests**: Run `xcodebuild test` from Terminal (or Cmd+U in Xcode). Expected: All error path tests pass with 0 failures.

**Build Infrastructure Review (review files, no execution needed):**

5. **build-dmg.sh**: Open `build-dmg.sh` and review the 8-step pipeline. Confirm it uses `xcodebuild -exportArchive` (not `codesign --deep`), `ditto` (not `zip`), and `notarytool` (not `altool`). Confirm `create-dmg` references the correct background and icon paths.

6. **release.yml**: Open `.github/workflows/release.yml` and review the CI pipeline. Confirm it triggers on `v*` tags, imports the certificate correctly, stores notarytool credentials, calls `./build-dmg.sh`, and cleans up the keychain in an `if: always()` step.

7. **ExportOptions.plist**: Confirm it has `method: developer-id`, `teamID: 5A7M476YY2`, and `signingStyle: automatic`.

**Note**: The build pipeline cannot be fully executed until the Developer ID Application certificate is set up. The verification here is a code/config review to confirm correctness before first use.
  </action>
  <verify>User confirms all 7 verification items pass or provides feedback on issues.</verify>
  <done>All error path scenarios verified by human. Build infrastructure reviewed and approved. Phase 9 complete.</done>
</task>

</tasks>

<verification>
Human has verified:
- Screen recording denial alert works correctly
- Missing API key triggers onboarding
- Upload error UI still functions
- Unit tests pass
- Build pipeline files are reviewed and correct
</verification>

<success_criteria>
- All error path scenarios verified by human
- Build infrastructure reviewed and approved
- Phase 9 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-distribution/09-03-SUMMARY.md`
</output>
